# name: CI
#
# on:
#   push:
#     branches: ["master"]
#   pull_request:
#     branches: ["master"]
#   workflow_dispatch:
#
# permissions:
#   contents: read
#
# jobs:
#   build:
#     name: Build (${{ matrix.name }})
#     runs-on: ${{ matrix.os }}
#     strategy:
#       matrix:
#         include:
#           - name: Windows x64
#             os: windows-latest
#             runtime: win-x64
#           - name: macOS ARM64
#             os: macos-latest
#             runtime: osx-arm64
#     steps:
#       - name: Checkout
#         uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
#
#       - name: Setup .NET
#         uses: actions/setup-dotnet@c2fa09f4bde5ebb9d1777cf28262a3eb3db3ced7 # v5.2.0
#         with:
#           dotnet-version: 10.0.x
#
#       - name: Publish
#         run: dotnet publish --runtime ${{ matrix.runtime }} -c Release
