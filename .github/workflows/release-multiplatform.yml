# name: Release
#
# on:
#   push:
#     tags:
#       - "v*"
#
# permissions:
#   contents: write
#
# jobs:
#   build:
#     name: Build (${{ matrix.name }})
#     runs-on: ${{ matrix.os }}
#     strategy:
#       matrix:
#         include:
#           - name: Windows x64
#             os: windows-latest
#             runtime: win-x64
#             artifact: PROJECT_NAME.exe
#           - name: macOS ARM64
#             os: macos-latest
#             runtime: osx-arm64
#             artifact: PROJECT_NAME
#     steps:
#       - name: Checkout
#         uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
#
#       - name: Setup .NET
#         uses: actions/setup-dotnet@c2fa09f4bde5ebb9d1777cf28262a3eb3db3ced7 # v5.2.0
#         with:
#           dotnet-version: 10.0.x
#
#       - name: Publish
#         run: dotnet publish --runtime ${{ matrix.runtime }} -c Release -o output
#
#       - name: Ad-hoc Code Signing
#         if: runner.os == 'macOS'
#         run: codesign --deep --force --verify --sign - output/${{ matrix.artifact }}
#
#       - name: Upload Artifact
#         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
#         with:
#           name: ${{ matrix.runtime }}
#           path: output/${{ matrix.artifact }}
#
#   release:
#     name: Create Release
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - name: Download Artifacts
#         uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
#         with:
#           path: artifacts
#
#       - name: Create GitHub Release
#         uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda # v3.0.0
#         with:
#           generate_release_notes: true
#           files: |
#             artifacts/win-x64/PROJECT_NAME.exe
#             artifacts/osx-arm64/PROJECT_NAME
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
